# 逻辑优化说明 v2.4.0

## 1. 睡眠保护（解决“睡醒发现提醒几十次”）

- 新增自动睡眠识别，设置页可开关：`自动识别睡眠：夜间屏幕熄灭且 15 分钟无步行时，暂停提醒`。
- 判定条件（全部满足才进入睡眠保护）：
  1. 当前时间在夜间宽窗口（20:00 - 10:59 之外不判定）；
  2. 屏幕处于熄灭状态；
  3. 最近 15 分钟没有步行事件。
- 进入睡眠保护后：
  - 不再发送全屏提醒、通知提醒和提前提醒；
  - 久坐计时基准持续刷新，**睡眠时长不计入久坐**；
  - 状态通知与首页显示“睡眠中，久坐计时与提醒已暂停”。
- 醒来后（屏幕亮起 / 白天 / 重新出现步行）：
  - 自动退出睡眠保护，并从醒来时刻重新开始久坐计时；
  - 不会补发夜间错过的提醒。
- 保留原有手动“免打扰时段”功能，两者互不冲突。

## 2. 低强度活动累计（解决“多次小幅活动仍被判久坐”）

- 原来只有“2 分钟内走满 120 步”才算有效活动，少量走动/伸懒腰/接水完全不产生任何抵消。
- 现在新增低强度活动累计：
  - 低频步行（步与步间隔 > 1.5 秒）计入低强度活动；
  - 加速度计检测到的明显晃动（起身、伸懒腰、接水等，间隔 ≥2 秒计一次）也计入；
  - 30 分钟窗口内累计达到有效步数的一半（最少 20 次），给当前久坐计时延长 **5 分钟**；
  - 每个久坐周期最多延长 4 次（合计最多 20 分钟），防止无限顺延；
  - 真正完成有效活动后，低强度累计清零并重新开始。
- 目的：不是把低强度活动当成达标，而是承认“动了总比没动好”，让提醒更贴合真实活动水平。

## 3. 版本

- versionCode 23 / versionName 2.4.0
- APK：`H:\SedentaryReminder\APK\SedentaryReminder-v2.4.0.apk`
- 设置页新增自动睡眠开关（默认开启）。

## 4. adb 实测记录（127.0.0.1:16448）

### 低强度活动累计
- 注入 `near` 状态（lastBreak = now - 33 分钟），启动服务。
- 通过仅 adb/root 可触发的 `TEST_SHAKE_BATCH` 注入 60 次低强度晃动。
- 结果：`lastBreak` 从 `08:45:30` 变为 `08:50:30`，**精确延长 300 秒（5 分钟）**。
- 随后立刻注入 240 次晃动，`lastBreak` 不再变化（0 秒），验证 **2 分钟冷却与防刷** 生效。

### 自动睡眠保护
- 注入 `near` 状态，设备时间调到 `23:00`，`input keyevent 223` 熄屏，确认 `mWakefulness=Asleep`。
- 时间跳到 `23:16`（模拟已睡 16 分钟），4 秒后检查：
  - `lastAlert=0`、`alertLevel=0`，**没有发任何提醒**；
  - 状态通知显示 `睡眠中，久坐计时与提醒已暂停`。
- `input keyevent 224` 唤醒屏幕，3 秒后检查：
  - 状态通知恢复 `已静坐 0 分 2 秒 / 40 分钟`；
  - `lastBreak` 已重置到唤醒时刻，没有补发夜间提醒。
- 测试后设备时间已恢复，并重新注入 `rich` 数据恢复监测状态。
