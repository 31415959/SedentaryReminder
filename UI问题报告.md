# 久坐提醒 App UI/UX 问题报告

## 设计方向
**温暖柔和（Warm & Soft）**
- 主色建议：暖米白/奶油底色，柔和浅绿或暖杏作为点缀
- 卡片：大圆角（20-24dp）、极浅暖灰阴影、少量留白
- 字体：无衬线、标题用中等/粗字重，正文用深灰（#333-#444）而非纯黑
- 按钮：主按钮柔和实色，次按钮浅描边或浅色底
- 避免：高饱和荧光绿、纯黑、锐利直角、过强阴影、密集小字

---

## 1. 首页（已查看手机模拟器截图）
### 已有问题
1. 色彩层级扁：背景、状态卡、白色统计卡对比度不足，整体“糊”在一起。
2. 状态卡绿色偏灰，缺少高级感；“正在监测”主标题不够突出。
3. 两张统计卡空洞：只有大数字 + 小标签，缺少图标/视觉锚点。
4. 底部“有效活动：2 分钟内累计 120 步……”像免责声明，挤在卡片底部。
5. 按钮：旧版本“开始/停止”权重相同；v2.2.3 已调整主次，但未最终视觉确认。
6. 进度条太细、存在感弱；可以考虑环形进度或更柔和的横向进度。

### 建议检查
- 状态卡是否需要加轻微呼吸/渐变
- 统计卡是否加简约线性图标（活动/闹钟）
- 底部说明是否改为卡片内弱化提示或展开帮助入口

---

## 2. 设置页（未做逐像素视觉确认）
### 预估问题
1. 内容长，缺少分段视觉节奏。
2. 滑杆、输入框、勾选框视觉风格不统一。
3. 自适应说明文字密度高，容易读不下去。
4. “个人档案”卡片与目标卡片之间缺少足够分层。
5. 免打扰时段、后台保活按钮较多，需要明确分组和主次。

### 建议
- 每个大分区用独立卡片 + 小标题 + 图标
- 输入框统一更暖的浅色底和更柔的圆角
- 自适应信息用可展开/折叠或小卡片展示，不一股脑丢出来

---

## 3. 统计页（未做逐像素视觉确认）
### 预估问题
1. 日/周/月/年切换按钮目前统一灰/绿，缺少选中态强调。
2. 月视图近 30 天，柱子可能过密；年视图 12 个月标签较小。
3. 图表颜色偏冷绿/灰，和“温暖柔和”方向需要统一。
4. 每日明细列表长，无分隔/图标，视觉累。
5. “分享本周报告”按钮位置和主次可以更弱化。
6. 自适应趋势图（绿线/灰线）需要更柔和的配色、更清楚的图例。

### 建议
- 图表改为暖色调低饱和配色
- 切换按钮做成“分段控件”或胶囊选中态
- 明细列表加轻分隔线与图标

---

## 4. 首启分步向导（未做逐像素视觉确认）
### 预估问题
1. 页面是程序化生成，样式可能太“空白”，缺乏品牌感。
2. 底部“跳过/上一步/下一步”三按钮权重需要明确：下一步主，跳过/上一步次。
3. 输入框/Spinner/CheckBox 风格需要统一为暖色浅底大圆角。
4. 完成页只有文字，缺少视觉鼓励元素。
5. 隐私说明可以更轻、更少。

### 建议
- 保持一页一问，但增加顶部步骤指示（圆点/进度条）
- 输入页加简单插画或图标占位
- 完成页使用柔和的庆祝感（圆角卡、暖色渐变极淡）

---

## 5. 侧边栏
### 已有问题
1. 菜单只有文字，缺少图标；当前选中态是高亮底色，可以更柔和胶囊化。
2. 顶部标题/副标语与菜单之间可增加呼吸感。
3. 底部版本号位置已经调整，但整体可以更轻盈。

### 建议
- 每项添加一个轻量线性图标（首页/统计/设置）
- 选中项使用浅暖色胶囊底 + 深色文字
- 抽屉右侧加浅描边或阴影分割

---

## 6. 全屏提醒 / 悬浮提醒
### 预估问题
1. 当前全屏提醒是深墨绿/深色底，偏“警报感”，与温暖柔和方向冲突。
2. 悬浮层同样深色，可能太重。
3. 全屏页“我知道/稍后提醒”按钮可以更柔和，增加呼吸感。
4. 提醒文案可更温和但依然清晰。

### 建议
- 全屏提醒采用“奶油色底 + 暖橙/暖绿强调”的柔和高对比，而不是黑底
- 悬浮层可半透明浅色卡片，中间偏下，不遮挡全部屏幕？
- 按钮用大圆角，主按钮暖色实色

---

## 7. 全局 / 技术约束
- 当前工程是纯原生 Java + 平台 XML，**没有 AndroidX**，构建用 aapt2 + javac + d8 + apksigner。
- 可改范围：`Project/app/src/main/res/layout/`、`drawable/`、`values/`、Java 中动态创建的 UI（OnboardingActivity）。
- 不得破坏现有 ID 关系，否则 MainActivity/Settings/Stats/Service 会找不到控件。
- 不改变业务逻辑：提醒算法、自适应、统计、保活、悬浮提醒等保持功能不变。

---

## 待确认清单（后续视觉复核）
- [ ] 设置页实际截图
- [ ] 统计页各视图实际截图
- [ ] 首启向导各页实际截图
- [ ] 侧边栏展开截图
- [ ] 全屏提醒/悬浮提醒截图
- [ ] 真机（荣耀 400 Pro 竖屏）最终效果确认
