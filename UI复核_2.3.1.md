# UI 复核记录 v2.3.1

## 总体结论
温暖柔和方向已经成立，主按钮颜色、数字统一、卡片层次均明显改善。
还需继续打磨：关键数字反馈、主按钮质感、卡片阴影、统计卡图标、测试提醒按钮形态。

---

## P0（必改，影响核心体验）

### 1. 关键数字缺少反馈高亮
- 当前“已静坐 0 分 0 秒”与统计数字都是同一深色，整体偏呆板。
- 建议：
  - 接近目标 / 达到目标 / 超时时，关键数字切换为主棕色或暖橙色。
  - 例如剩余时间后段、超时后数字变为暖色。

### 2. 主按钮略显沉闷
- 当前为纯色摩卡棕 `#8B6F5B`，大块纯色缺少通透感。
- 建议：
  - 增加极淡的上浅下深渐变。
  - 或给按钮文字加非常柔和的阴影。
  - 保留当前主色，不要回到亮橘色。

---

## P1（提升空间感与聚焦）

### 3. 卡片阴影还是太浅
- 层次比之前好，但卡片更像“贴”在背景上，悬浮感不足。
- 建议：
  - 加深并扩散阴影。
  - 阴影可带一点暖色，不纯黑。
  - 或略微降低背景明度，提高卡片与背景分离度。

### 4. 顶部卡片间距稍松散
- 状态卡与统计卡之间垂直留白偏大。
- 建议：
  - 适当收紧顶部区域间距。
  - 让状态、统计、控制形成更聚焦的信息组。

---

## P2（细节打磨）

### 5. 统计卡片内容单调
- 当前只有大数字 + 下方小标签。
- 建议：
  - “今日有效活动”配走动小人/步数图标。
  - “今日提醒”配铃铛/提醒图标。
  - 线条图标用棕色系，不要彩色。

### 6. “测试提醒”按钮太普通
- 当前已是透明文字链接，但看起来像普通说明文字。
- 建议：
  - 做成浅色胶囊按钮，与“停止”形成对称。
  - 或下方加一条极淡分隔线，保留弱化层级。

---

## 修改记录（本轮已落地，待视觉复验）

> 范围：首页 `activity_main.xml`、`MainActivity.java` 及相关 drawable/values；未改业务逻辑与控件 ID。
> 验证：Gradle `assembleRelease` 构建成功，APK 已签名并安装到 emulator-5556；UI 层级 dump 与截图像素采样通过，视觉模型复核因预算耗尽未完成。

- [x] **1. 关键数字反馈高亮**：`MainActivity.updateUi()` 改为 SpannableString 高亮时间数字；正常监测用主棕 `#8B6F5B`，进度 ≥80% 用暖橙 `#C56F2B`，已超时/提醒已发出用深橙 `#B85C1E`；状态标题、两个统计数字、进度条同步联动。像素采样：tvDetail 中深灰字与棕色数字并存，tvHomeBreaks 数字为棕色。
- [x] **2. 主按钮质感**：`btn_primary.xml` 增加上浅下深三色渐变 `#937760 → #8B6F5B → #80624F`；`btnStart` 增加 `elevation 6dp` 与极淡文字阴影，保留摩卡棕主色。像素采样：按钮上/中/下为 `#92765F / #8B6F5B / #826451`。
- [x] **3. 卡片阴影**：`card_white.xml` 改为 layer-list 暖棕阴影（3 层 `#6B4D33` 渐变叠层 + 白色表面），并配合 `values/styles.xml`、`values-v28/styles.xml` 提升 elevation（10dp/12dp）；页面底色由 `#F3EDE0` 微调为 `#F1E9DA`。像素采样：统计卡边缘出现暖棕阴影带，左侧 `#E1D7C7`、底部 `#DACFBE`，与底色 `#F1E9DA` 已明显分离。
- [x] **4. 顶部间距**：状态卡 bottom padding 22dp→18dp；状态→统计、统计→控制间距 12dp→10dp。
- [x] **5. 统计卡图标**：新增 `ic_steps.xml`（走动小人）、`ic_bell.xml`（铃铛），棕色线条 22dp，位于两张统计卡数字行右侧。UI dump 确认两张卡右侧均存在 ImageView。
- [x] **6. “测试提醒”按钮**：新增 `btn_ghost.xml` 浅色胶囊底 + 浅描边，字号 13sp→14sp，与“停止”同形对称、层级弱于主按钮。UI dump：停止 `[48,701][351,773]`、测试 `[369,701][672,773]`，同高同宽；像素采样背景为 `#F5EDE0`。

### 构建与安装
- APK：`H:\SedentaryReminder\APK\SedentaryReminder-v2.3.1-ui2.apk`（63,464 字节，签名 `CN=Sedentary Reminder`，versionCode 21 / versionName 2.3.1）。
- 已安装至 `emulator-5556`（Android 15，720x1280）。
- APK 已验证：`apksigner verify` 通过，包名 `com.sedentary.reminder`，minSdk 26 / targetSdk 36。


## 其他页面待复核
- [ ] 设置页
- [ ] 统计页（日/周/月/年）
- [ ] 首启分步向导
- [ ] 侧边栏展开状态
- [ ] 全屏提醒 / 悬浮提醒

## 参考文件
- 完整问题报告：`H:\SedentaryReminder\UI问题报告.md`
