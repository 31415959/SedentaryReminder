# UI 复核记录 v2.3.1-ui2（再复核）

> 复核对象：`APK\SedentaryReminder-v2.3.1-ui2.apk`（SHA256 `5FD2BBAB…DC80C6`，versionName 2.3.1，versionCode 21）
> 复核方式：模拟器（127.0.0.1:16448，1440x2560）实测截图 + 像素采样 + dex 反编译验证，未改动任何代码。
> 本次新截图：`screenshots/qa_rev1.png`（0 分 0 秒）、`qs_rev2` 修正为 `qa_rev2.png`（82% 进度橙色态）、`qa_stats1.png`（统计页）、`qa_settings.png`（设置页）、`qa_drawer2.png`（抽屉）。

---

## 总体结论

上一轮 UI 复核中"关键数字高亮"**确认已实现且运行正常**（曾有疑问，本次像素实测证伪）；主按钮状态逻辑（P0-1）**确认仍然存在**；进度条在 0% 时存在感问题（P0-2）确认。**新增重要发现：统计页/设置页存在系统默认绿色（#1E6B52）残留，与"温暖柔和"方向冲突；状态栏浅底白字几乎不可读。**

---

## ✅ 已确认正常（本轮实测，勿重复修改）

1. **关键数字反馈高亮正常**：dex 反编译 `renderDetail` 字节码正确；实测 0% 进度时"0 分 0 秒"= 棕色 `#8B6F5B`（qa_rev1 像素证据），82% 进度时"33 分 9 秒"= 橙色 `#C56F2B`（qa_rev2）。上一轮记录无误。
2. **进度条颜色随状态联动**：82% 时橙色进度条醒目（qa_rev2）；进度条 tint 键 `progressColor` 逻辑正确。
3. **统计卡数字随状态联动**：qa_rev2 中两张卡的"0"均为橙色。
4. **图标线条粗细统一**：`ic_steps.xml` / `ic_bell.xml` stroke 均为 1.5dp，棕色 `@color/accent_brown`。
5. **卡片阴影**：暖棕分层阴影在卡片边缘可见（页面底色与卡片分离度已可接受）。
6. **抽屉**：选中胶囊态、副标语、底部版本号均正常。

---

## P0（确认存在，优先修）

### P0-1 状态与按钮逻辑冲突 —— ❌ 确认存在（最严重）
- 证据：qa_rev2 实测"正在监测"状态下，主按钮仍显示"开始监测"；点击会再次走 `startMonitoring()`（重复启动语义，用户感知为逻辑错误）。
- 根因：`MainActivity.updateUi()` 从未设置 `btnStart` 的文本/行为，XML 写死"开始监测"。
- 连带问题：
  - 未监测时"停止"按钮仍显示且可点（无意义操作）；
  - 监测中"开始监测"与"停止"两个停止含义的按钮并存，语义重复；
  - UI 层次上主按钮（开始/停止）应当随状态切换，`btnStop` 的独立存在需重新设计（建议：监测中主按钮=停止监测，未监测时主按钮=开始监测；`btnStop` 弱化或移除）。

### P0-2 顶部状态卡下方空白过大、进度条存在感弱 —— ❌ 确认存在
- 证据：0% 时进度条轨道 `#E5D8C3` 与状态卡面 `#F0E8DB` 像素几乎同色（qa_rev1 中肉眼几乎不可见，仅一条极淡的线）；82% 时橙色条醒目（qa_rev2）。
- 判断：**"存在感弱"主要不是粗细（已 8dp），而是轨道对比度不足 + 无圆角**；顶部"空白"来自状态卡下 padding、卡间 10dp margin 与约 25dp 阴影带的叠加。

---

## P1

### 3. "停止"/"测试提醒"按钮层级仍偏高 —— ❌ 确认
- 两按钮同宽同高（`[48,701][351,773]` 与 `[369,701][672,773]` 级），视觉重量几乎相同；"停止"白底 `#FFFCF7` 置于白色控制卡内，仅靠 1dp 描边区分，"测试提醒"淡米胶囊。两者都"轻"于主按钮但层级仍未拉开，且未监测时"停止"不应出现。

### 4. 统计卡数字+图标居中对齐 —— ❌ 未落实
- 当前为：数字靠左、图标靠右、标签靠左，三者无居中组合；"走动小人"与"铃铛"的垂直视觉重心不一致（全身人像 vs 半高铃铛），图标行与数字行无对齐锚点。

### 5. 状态文字颜色统一 —— ⚠️ 部分达成
- 现状："正在监测"标题棕色；"已静坐 …，目标 …"正文灰绿 `#3F4944` + 时间数字棕/橙，呈现"双色混合"；与"整段统一深棕、时间数字加粗"的要求不符（时间数字已着色但未加粗）。
- 注：如需保留"接近目标转橙"反馈，可在统一深棕基础上叠加近阈值的整体升档；两者不冲突。

### 6. 底部说明文字太弱 —— ❌ 确认
- `tvHomeHint`：#A0968A / 12sp，与底色对比低；两行换行后第二行贴近卡片底边（padding 16dp 内），显得拥挤且次要。

---

## P2

### 7. 图标线条粗细 —— ✅ 已符合（1.5dp 统一）
### 8. 菜单图标 / 状态栏过渡 —— ⚠️ 部分，且有新问题
- 菜单 `☰` 为文本按钮（20sp），可换更轻量矢量图标（可选）。
- **状态栏可读性差（新增）**：状态栏为浅米底（约 `#EFE7D8`），系统时间/信号/电量图标为白色，**几乎不可读**（qa_rev1/qa_rev2/qa_stats1/qa_drawer2 均可见）；应为深色状态栏图标（`SYSTEM_UI_FLAG_LIGHT_STATUS_BAR` 或 `windowLightStatusBar`）。
- **顶栏与页面底色割裂**：top_bar 背景 `#F5F6F4`（冷灰白）与页面 `#F1E9DA`（暖米）色温不一致，视觉上顶栏是"贴上去的灰色带"。
### 9. 卡片阴影 —— ✅ 可接受（暖棕分层，无需再调）

---

## 🌟 新发现问题（此前报告未记录）

### A. 统计页"今日有效活动"数字为深绿（同层级颜色不一致）—— 硬伤
- `activity_stats.xml` L70 硬编码 `android:textColor="#1E6B52"`；同排"今日提醒"数字为 `#3F362E`（L97）。**同一行两张卡的数字颜色不一致（左绿右棕黑）**，且绿色与全局暖棕体系冲突。
- 实测：qa_stats1.png 像素采样 `#1E6B52` 确认。

### B. 设置页滑动条与勾选框为系统默认绿色 —— 硬伤
- `activity_settings.xml`：3 个 SeekBar 均硬编码 `progressTint="#1E6B52"` + `thumbTint="#1E6B52"`（L108/109、L144/145、L180/181）；CheckBox（L77、L231）未设 `buttonTint`，显示系统默认绿色勾选。
- 与"温暖柔和 / 避免高饱和荧光绿"方向直接冲突；UI问题报告第 2 节预估问题（滑杆/输入框/勾选框风格不统一）原样保留，未落实。

### C. 统计页"最近一周"摘要区排版
- 文字全部 18sp 粗体深棕（`tvWeekSummary` 单行换行），数字与位次无主次区分；"累计久坐 0 分钟 · 平均每次 数据积累中"断行后"中"字单独成行，观感破碎。
- 建议：数值与标签分离、控制每行字数。

### D. 统计页"自适应调整趋势"卡片布局失衡
- 图例文字居左，折线图（40-40 两点）紧贴卡片右下角，卡片主体大面积空白；日期轴（08/16…08/22）在卡片底部与图表分离。
- 建议：图表区域居中并占满卡片可用高度，图例/图轴对齐。

### E. 工程与交接问题（与 UI 无关但影响交付）
- **资源文件未入库**：`colors.xml`、`styles.xml`、`values-v28/`、`btn_ghost.xml`、`ic_steps.xml`、`ic_bell.xml` 均为未跟踪（`git status` `??`），HEAD（v2.3.1 提交）缺失它们——**全新 clone 无法编译**。
- **构建管线描述与实际不符**：交接说明写"手动 AAPT2 + javac + d8 + apksigner"，但 APK 内含 `META-INF/com/android/build/gradle/app-metadata.properties`，实际为 **Gradle 构建**（app/build.gradle 为 AGP 8.x 配置，JDK `BuildEnv/jdk21`）。
- 工作区 `MainActivity.java` 修改时间（00:16）晚于 v2.3.1 提交（00:07）但早于 APK（11:05），构建时源码状态无法从 git 复现。

---

## ui3 修改记录（已按本轮意见完成）

> 构建：Gradle `assembleRelease` 成功；APK `H:\SedentaryReminder\APK\SedentaryReminder-v2.3.1-ui3.apk`（64,968 字节），已安装 `127.0.0.1:16448` 与 `emulator-5556`。
> 验证：uiautomator 层级 dump + 像素采样 + 主按钮点击状态切换实测；本轮未使用视觉模型。

- [x] **P0-1 按钮状态逻辑**：主按钮随状态切换，未监测=“开始监测”，监测中/活动/提醒/超时=“停止监测”；点击按当前状态执行 `startMonitoring()` / `stopMonitoring()`；独立的“停止”按钮已移除。
- [x] **P0-2 进度条与顶部空白**：新增 `progress_horizontal.xml`（4dp 圆角），轨道 `#D8C5A6` 与卡片 `#F0E8DB` 已拉开对比；状态卡 bottom padding 18→12dp，状态→统计间距 10→6dp，统计→控制 10→8dp。
- [x] **P1-3 按钮层级**：主按钮仍为唯一实色主操作；“测试提醒”改为居中窄胶囊 TextView（宽约 128dp、高约 49dp），层级明显低于主按钮。
- [x] **P1-4 统计卡对齐**：数字与图标组成水平居中的组合，标签在下方居中；两卡结构与 padding 一致。
- [x] **P1-5 状态文字统一**：`tvDetail` 正文改为深棕 `#3F362E`；时间数字加粗并保留棕/橙反馈色；接近目标/超时时整段升为橙色。
- [x] **P1-6 底部说明**：`tvHomeHint` 12sp→13sp，颜色 `#A0968A`→`#7C7163`，行距 2dp→3dp，控制卡底部 padding 16→18dp。
- [x] **P2-8 菜单与状态栏**：菜单 `☰` 文本按钮改为 `ic_menu.xml` 矢量线条图标；新增 `AppTheme`/`AppTheme.Fullscreen`，`windowLightStatusBar=true`、状态栏/导航栏底色统一为 `#F1E9DA`；`top_bar` 背景 `#F5F6F4`→`#F1E9DA`。
- [x] **新发现 A**：统计页“今日有效活动”数字 `#1E6B52`→`@color/text_title`，与“今日提醒”一致。
- [x] **新发现 B**：设置页 3 个 SeekBar 的 progress/thumb tint 全部 `#1E6B52`→`@color/accent_brown`；2 个 CheckBox 增加 `buttonTint="@color/accent_brown"`。
- [x] **新发现 C**：周摘要拆为两行 `tvWeekSummary1`（16sp 深棕粗体）/ `tvWeekSummary2`（13sp 灰棕），避免“中”字孤行。
- [x] **新发现 D**：趋势图高度 170→190dp，纵轴最大值按数据动态取整（最小值 60），两点数据不再贴底；图例改为整行居中。
- [x] **新发现 E**：`交接说明.md` 构建方式已更正为 Gradle；新增资源与源码改动已 `git add` 并提交，修复全新 clone 无法编译的问题。

### 仍待复核
- 统计页日/周/月/年切换态与每日明细。
- 首启分步向导、全屏提醒/悬浮提醒。
- 真机荣耀 400 Pro / MagicOS 10 的状态栏与整体观感。


## 未覆盖项（下轮建议）

- 统计页日/周/月/年切换态、每日明细列表（本次仅默认周视图）。
- 首启分步向导（OnboardingActivity，需清数据后进入）。
- 全屏提醒 / 悬浮提醒（AlertActivity / overlay_alert，需触发提醒或"测试提醒"按钮）。
- 真机荣耀 400 Pro / MagicOS 10 状态栏与字体渲染（模拟器与真机有差异）。

---

## 附：本次复核操作记录

- 临时将设备 `127.0.0.1:16448` 的 `lastBreak` 置为 33 分钟前以复现 82% 进度态（qa_rev2），**已恢复**原 prefs（删除该键，app 重新启动）。
- 核对用截图见 `screenshots/qa_*.png`。
