# 文案与排版审查（v2.3.2 验证结果）

> 复核对象：`APK\SedentaryReminder-v2.3.2.apk`（versionCode 22 / versionName 2.3.2），提交 `153c7e2`。
> 方法：源码 diff 核对 + 模拟器实测截图（screenshots/v232_*.png、ui6_*.png）。

## 结论

**A1-A5、B4-B10、C1-C4 全部修复并经实测确认；C5（覆盖前确认提示）未实现；1 个 P3 级观感项遗留。** 可以发布。

## ✅ 逐项确认

| 项 | 修复 | 实测证据 |
|---|---|---|
| A1 "您已静坐约 0 分钟" | "您已静坐不到 1 分钟。"（mm==0 分支） | 源码确认 |
| A2 "床到桌" | "拿手机、翻身等短距离走动不计入"（与首页表述统一） | 源码确认 |
| A3 "起来走 2 分钟" | "起来活动片刻：2 分钟内累计 120 步"（与达标标准一致） | 源码确认 |
| A4 "7天响应率" | "7 天响应率"+（空格）；"实际目标"独立成行，58% 不再孤行 | 源码确认 |
| A5 版本号 | versionCode 22 / versionName 2.3.2；抽屉版本号改动态读取 PackageInfo | 抽屉截图 v2.3.2 ✅ |
| B4 免打扰建议 | hscore 夜间 0-5 点改为 2,3,2,3,2,3（连续 ≤3）→ 建议可触发 | 设置页显示"0:00 - 6:00"+"采用建议时段"按钮 ✅ |
| B6 摘要孤行 | 改为三列数字卡（23 次/19 次/12 天 + 标签），无断行 | 统计页实测 ✅ |
| B7 诊断区 | 分行"…7 天响应率 58% / 实际目标 40 分钟" | 源码确认 |
| B8 抽屉 | 三项加棕色线性图标（drawableStart+tint），背景 #FFF→card_bg | 抽屉实测 ✅ |
| B9 月视图标签裁切 | 标签 x 用 measureText 半宽夹紧在卡片内 | 08/22 完整显示 ✅（末两标签间距偏近，见遗留） |
| B10 月视图柱密度 | dense（≥30）柱宽 5dp、间隙 1.5dp | 月视图实测：60 柱清晰 ✅ |
| C1 文档"约 70%" | 近 7 天固定 14 ok / 6 miss = 精确 70% | 源码确认 |
| C2 真机不可用无提示 | 加 `id -u` root 检查，非 root 明确 throw（提示 adb root） | 源码确认 |
| C3 假成功 | $ErrorActionPreference=Continue；push/cp 检查 $LASTEXITCODE；写入后回读 SEED_OK 校验 | 源码确认 |
| C4 注入后自毁 | 今天 target 直接设 40（= effectiveSitMinutes 稳定值），service 覆盖后趋势图不变 | 统计页趋势图 08/22=40 ✅ |

## ⚠️ 遗留（均为低优先）

1. **C5 覆盖前确认**：脚本仍直接整体替换 prefs，无交互警告/确认（有 root 检查与文档说明兜底）。若在演示/真机使用建议加"将覆盖设备现有数据"提示。
2. **月视图末段标签间距**：labelStep=5 下最后一段仅 4 天，08/18 与 08/22 两标签间距约 20px（原始像素），观感贴连（不再裁切，P3）。可选优化：末段按实际天数自适应间隔。

## 备注

- `tvWeekSummary1` 已无残留引用（重命名后仅剩 tvWeekSummary2）。
- 抽屉版本号动态化后，nav_drawer.xml 初始 text 为空串，各页由 SideNav 填充，无硬编码版本残留。
- 月/周/日/年四个视图、抽屉、设置免打扰卡、统计摘要均已截图留档（screenshots/ 下 ui6_*、v232_*）。


## v2.4.0 追加

- C5 已补：脚本新增 `-Force`，目标设备已有 prefs 时未传 `-Force` 会直接拒绝覆盖。
- 月视图末段标签：`labelStep` 30 天从 5 改为 6，末段间隔更均匀。
- 新增自动睡眠保护与低强度活动累计，详见 `逻辑优化说明_2.4.0.md`。
- versionCode 23 / versionName 2.4.0。
