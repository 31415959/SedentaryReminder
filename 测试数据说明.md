# 测试数据注入说明

用于在模拟器/真机上快速注入一整段历史数据，模拟首页各种状态、统计页日/周/月/年、趋势图、设置页自适应等场景，不修改 App 业务代码。

## 使用方法

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File H:\SedentaryReminder\tools\seed_test_data.ps1 `
  -Serial '127.0.0.1:16448' -State rich -ShotDir 'H:\SedentaryReminder\screenshots\seed'
```

参数：
- `-Serial`：adb 设备序列号，默认 `127.0.0.1:16448`。
- `-State`：注入场景，见下表。
- `-ShotDir`：可选，注入并启动首页后截图保存目录。
- `-NoStart`：只写数据并停止 App，不自动启动。

## 场景

| State | 首页状态 | 说明 |
|---|---|---|
| `rich` | 正在监测，刚计时 | 注入完整 365 天统计 + 7 天自适应 + 24 小时评分，用于统计页/设置页 |
| `zero` | 正在监测 0 分 | 数据同 rich，但 `lastBreak=now` |
| `near` | 正在监测 33 分 | 目标 40 分钟，约 82%，数字/进度应转暖橙 |
| `overtime` | 已超时（短暂） | `lastBreak` 提前 55 分钟；服务启动后通常立刻发提醒，随后会变为“提醒已发出” |
| `alert` | 提醒已发出 | `lastBreak` 提前 55 分钟，`lastAlert` 提前 2 分钟 |
| `stopped` | 未开始 | `enabled=false`，统计卡显示“—”，主按钮为“开始监测” |
| `clean` | 未开始，无历史 | 清空所有历史统计，仅保留默认档案/设置 |

## 注入的数据内容

- 档案/设置：30 岁、170cm、65kg、久坐办公、目标 40 分钟、窗口 2 分钟/120 步、免打扰 23:00-8:00、自适应开启。
- 365 天每日数据：`breaks`、`alerts`、`respOk`、`respMiss`、`sitSumSec`、`sitCount`；含周末低谷、工作日高峰、每 12 天一个空档日。
- 最近 7 天自适应快照：目标 30/35/40/45/50/40/35，响应分 3/5/8/6/7/4/9，步数 80-130。
- 24 小时评分：夜间低分（2-4）、上午/下午高分（7-9）、其余中性；四时段评分 3/7/6/4。
- 最近 7 天响应率控制在约 70%，保证有效目标稳定在 40 分钟，便于复现 82% 阈值。

## 当前模拟器实测结果（127.0.0.1:16448）

- 首页 `zero`：`已静坐 0 分 7 秒，目标 40 分钟`
- 首页 `near`：`已静坐 33 分 9 秒，目标 40 分钟`
- 首页 `alert`：`提醒已发出，等待起身 2 分 8 秒`
- 首页 `stopped`：`未开始 / 开始监测`，统计卡为 `—`
- 统计页周摘要：`有效活动 23 次 · 提醒 19 次 · 连续达标 12 天`、`累计久坐 975 分钟 · 平均每次 42 分钟`
- 统计页日/周/月/年标题已分别切换为 `今日 24 小时`、`近 7 天`、`近 30 天`、`近 12 个月`

截图目录：`H:\SedentaryReminder\screenshots\seed\`
- `seed_rich.png` / `seed_zero.png` / `seed_near.png` / `seed_overtime.png` / `seed_alert.png` / `seed_stopped.png`
- `seed_stats.png`、`seed_settings.png`
- `seed_stats_day.png` / `seed_stats_week.png` / `seed_stats_month.png` / `seed_stats_year.png`
- `seed_stats_month_ui5.png`（ui5 修复 30 天标签重叠后的版本）

## 恢复干净状态

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File H:\SedentaryReminder\tools\seed_test_data.ps1 `
  -Serial '127.0.0.1:16448' -State clean
```

`clean` 会清空历史统计（SharedPreferences 文件整体替换），App 保持未开始状态。
