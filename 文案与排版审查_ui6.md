# 文案与排版审查（v2.3.1-ui5/ui6 结果）

> 针对 ui4 审查清单的修复验证。ui5=`aad8dc4`（图表标签稀疏化），ui6=`b47a61a`（柱顶留白）。

## ✅ 已修复并实测确认（统计图表排版）

| 项 | 结果 |
|---|---|
| B1 日视图 x 轴粘连 | ✅ 每 4 小时一个标签（0/4/8/12/16/20 时），"10时12时"粘连消除（seed_stats_day_ui6.png + 实测） |
| B2 年视图 x 轴粘连 | ✅ 跳月显示（09/11/01/03/05/07/08 月），"09月10月"粘连消除（实测 ui6_year2） |
| B3 柱顶数值拥挤 | ✅ `top` 18→24dp，最高柱值有留白；数值标注稀疏化（仅 mark 列，>0） |
| B5 无数据时"0 0"满屏 | ✅ 数值仅 `>0` 时绘制（零值不标注） |
| 周视图 | ✅ 7 天标签完整无粘连，双柱数值正常 |

`labelStep`：≥30→5、≥20→4、≥12→2、否则 1；`showMark = i%step==0 || i==last`。

## ⚠️ 新发现（ui6 引入/暴露）

| # | 位置 | 问题 |
|---|---|---|
| B9 | 月视图 x 轴 | 最后一个标签 "08/22" 被卡片右缘**裁切**，显示为 "08/2"（`showMark` 含 last，末槽中心距右缘仅 slot/2≈26dp，标签宽约 40dp 溢出）。建议：末标签改居中偏移或省略 last |
| B10 | 月视图柱体密度 | 30 天 × 双柱 = 60 根柱挤一卡，观感"密林"；数值稀疏标注下对应关系不直观。可接受（数据固有密度），可选优化：月视图柱宽上限减半 |

## ❌ 未修复（审查清单遗留，git 确认未动）

- **A1** "您已静坐约 0 分钟"（AlertActivity）
- **A2** "床到桌的几米走动不算" 与首页 "短距离走动不计入" 表述不一致（AlertActivity）
- **A3** "起来走 2 分钟" 与实际标准"2 分钟内 120 步"不符（MonitorService）
- **A4** 设置页"7天响应率"缺空格＋断行孤行"58%"（SettingsActivity）
- **A5** versionName 仍 "2.3.1"，未随 ui5/ui6 跟进（build.gradle）
- **B6** 统计摘要"连续达标 / 12 天"孤行（activity_stats.xml 16sp 超宽）
- **B4/C1-C4** 注入工具：hscore 无法触发免打扰建议、假成功（退出码未检查）、"约 70%" 写 58%、注入后 today 被 snapshotAdaptive 覆盖、无覆盖确认
- **B8** 抽屉菜单无图标（低优先）


## v2.3.2 修复记录（本轮已改）

- [x] A1：AlertActivity 0 分钟时显示“您已静坐不到 1 分钟。”；实测确认。
- [x] A2：全屏提醒统一为“拿手机、翻身等短距离走动不计入”；实测确认。
- [x] A3：第 3 次提醒文案改为“起来活动片刻：2 分钟内累计 120 步”（动态读取实际标准）。
- [x] A4：“7天响应率”→“7 天响应率”，实际目标单独一行；实测 70% 显示正常。
- [x] A5：versionCode 21→22，versionName 2.3.1→2.3.2；抽屉版本号改为动态读取，实测显示 v2.3.2。
- [x] B6：周摘要改为三列数字+标签（23 次 / 19 次 / 12 天），不再断行孤行；实测三列独立。
- [x] B9：末标签按文字宽度夹紧在图表内，月视图 08/22 完整显示；视觉复核通过。
- [x] B10：≥30 天时柱宽 12dp→5dp、柱距收窄，月视图不再“密林”；视觉复核通过。
- [x] B4/C1：注入工具夜间 hscore 全部 ≤3（0-5 时 2/3 交替），实测触发建议“0:00 - 6:00”；近 7 天响应率固定 14/6=70%，实测显示 70%。
- [x] C2/C3：脚本增加 root 检查、push/cp 退出码检查和写入后验证，失败会明确报错。
- [x] C4：今天 adp_target 注入值改为 40，与 service 启动后的 snapshotAdaptive() 结果一致。
- [x] C5：文档明确说明会整体覆盖 SharedPreferences，仅用于测试设备。
- [x] B8：抽屉三项增加线性图标（home/stats/settings）并统一暖棕 tint。
