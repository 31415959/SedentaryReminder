# 久坐提醒 App 项目交接说明

## 项目概况
- 目的：Android 久坐提醒 App，监测步数/运动，久坐超时提醒，自适应调整提醒策略。
- 当前最新版本：`v2.4.0`（versionCode 23，APK 已生成，源码已提交）
- 仓库：`H:\SedentaryReminder`
- GitHub：`https://github.com/31415959/SedentaryReminder`
- 目标机型：荣耀 400 Pro / MagicOS 10 / Android 16
- 构建环境：`H:\SedentaryReminder\BuildEnv`（JDK21 + Android SDK + Gradle + 签名密钥）
- 构建方式：Gradle 8.13 + AGP 8.x（`Project/app/build.gradle`），无 AndroidX，纯原生 Java + XML；签名 `BuildEnv\keystore\release.jks`
- APK 输出：`H:\SedentaryReminder\APK`

## 视觉复核工作流（重要）
- 本地截图文件路径不能直接被模型“看见”，必须：
  1. 截图作为聊天附件上传（图片块进请求），最稳
  2. 或使用平台提供的图片读取工具把原图作为 tool-result 图片塞回请求
- 不要依赖 `vision_describe` 作为唯一看图方式；该工具只返回文本描述，且后端不稳定。
- 当前会话工具列表中没有 `read_image`，只有 `vision_*` 系列；因此上传附件是首选验证方式。

## 当前 UI 状态
- 已完成温暖柔和方向改造：米色/奶油底、摩卡棕主色、圆角卡片、柔和阴影、统计卡图标、主按钮渐变。
- 最新复核（基于手机竖屏模拟器截图）确认：整体已达到“温暖柔和 + 高级”基本要求。
- `v2.4.0` 在 v2.3.2 UI/文案修复基础上，新增自动睡眠保护和低强度活动累计，解决夜间连续提醒与“小幅活动仍判久坐”问题；详见 `逻辑优化说明_2.4.0.md`。

## 待修问题
### P0
1. **状态与按钮逻辑冲突**
   - 状态显示“正在监测”时，主按钮仍是“开始监测”。
   - 应改为：监测中主按钮显示“停止监测/暂停”，未监测时显示“开始监测”。
2. **顶部状态卡下方空白过大**
   - 进度条太细、存在感弱。
   - 建议加粗进度条或加动态呼吸/进度视觉。

### P1
3. **“停止”和“测试提醒”按钮层级仍偏高**
   - 停止可改为浅棕描边；测试提醒改为文字链/淡胶囊。
4. **小卡片细节**
   - 数字+图标居中对齐；统一卡片 padding。
5. **状态文字颜色统一**
   - “正在监测”与“已静坐 0 分 0 秒”颜色应统一为深棕，时间数字可加粗。
6. **底部说明文字太弱**
   - 加深颜色或加小信息图标。

### P2
7. 图标线条粗细统一。
8. 左上角菜单图标可更轻量；状态栏与背景过渡可做沉浸式。
9. 卡片阴影可再柔和加深。

## 测试数据注入
- 脚本：`H:\SedentaryReminder\tools\seed_test_data.ps1`，可注入 365 天历史统计并切换首页状态（rich/zero/near/overtime/alert/stopped/clean）。
- 说明与实测结果：`H:\SedentaryReminder\测试数据说明.md`。
- 当前 `127.0.0.1:16448` 与 `emulator-5556` 均已注入 `rich` 数据并恢复监测中状态。

## 已有报告文件
- `H:\SedentaryReminder\UI问题报告.md`（完整问题清单）
- `H:\SedentaryReminder\UI复核_2.3.1.md`（v2.3.1 复核记录）
- `H:\SedentaryReminder\UI复核_2.3.1-ui2.md`（ui2 再复核记录 + ui3 修改记录）
- `H:\SedentaryReminder\screenshots\`（模拟器截图）

## 业务功能现状
- 久坐提醒、有效活动判定、自适应提醒、免打扰、统计日/周/月/年、趋势图、悬浮/全屏提醒、个人档案分步向导、GitHub Release。
- 所有功能保持不动，本轮只做 UI/UX 视觉优化。

## 新对话建议第一步
1. 阅读本文件。
2. 先修 P0（按钮状态逻辑 + 顶部空白）。
3. 再修 P1/P2。
4. 构建最新 APK，并在手机模拟器（`127.0.0.1:16448` 或 `127.0.0.1:16416`）安装验证。
5. 如果你需要视觉复核，截图作为附件上传；不要依赖 `vision_describe`。
