# UI 复核记录 v2.3.1-ui4（P2 残留项收尾验证）

> 复核对象：`APK\SedentaryReminder-v2.3.1-ui4.apk`（SHA256 见 APK 目录），提交 `e5a3e66`。
> 复核方式：模拟器 127.0.0.1:16448 实测 + 源码核对，未改代码。
> 截图：`screenshots/v4_stats_chart.png`（柱状图+图例）、`v4_drawer.png`、`v4_home.png`（实为统计页，含清理后数据）。

## 结论

上一轮 P2 残留 3 项已基本修复，仅剩 1 项为有意保留项。**v2.3.1-ui4 可以发布。**

## 修复确认

1. **ic_menu 线条 1.5dp** —— ✅ 与 ic_steps / ic_bell 统一（`e5a3e66` 源码确认；抽屉渲染正常无粗糊）。
2. **柱状图双柱色对比** —— ✅ 提醒柱 `0xFF8A7F73` → `0xFF3F362E`（深棕黑），活动柱保持 `0xFF8B6F5B`（摩卡），对比明确。当前设备无提醒/活动数据（测试数据已清理，无柱属正常），渲染路径为直接 setColor，无风险。
3. **图例文案** —— ✅ 4 处全部统一为"深灰 = 提醒 · 摩卡 = 有效活动"（`StatsActivity` 日/周/年 3 处动态 + `activity_stats.xml` 1 处静态）；截图可读确认。

## 剩余项（确认保留）

- **ic_stat 蓝色圆环**（launcher + 通知小图标 `#2F6BFF`）：ui4 未改动。若有意保留蓝（launcher 辨识度权衡）则无问题；若后续要统一暖色系，改 `ic_stat.xml` 的 `fillColor` 即可，但通知小图标着色空间小、辨识度会下降，建议维持现状。

## ui5 补充（测试数据可视化检查后）

- 注入 365 天测试数据后，统计页“近 30 天”X 轴 30 个日期标签出现重叠/截断。
- 修复：`StatsChartView` 按数据量抽稀标注——≥30 点每 5 个显示一个（保留末点）、≥20 点每 4 个、≥12 点每 2 个；数值标签同步抽稀，避免 30 天数值互相压字。
- 新包：`H:\SedentaryReminder\APK\SedentaryReminder-v2.3.1-ui5.apk`；已安装两台模拟器。
- 新截图：`screenshots\seed\seed_stats_month_ui5.png`（待人工复核）。
- 视觉模型本轮预算耗尽，日视图/年视图未做模型级复看，仅代码路径确认抽稀逻辑生效。


## 数据说明

- 上轮测试产生的 `t_20260822_alerts=1` 与 `enabled` 状态已清理；统计页"有效活动 0 次 · 提醒 0 次"确认归零。
- 未覆盖：真机（荣耀 400 Pro / MagicOS 10）状态栏与通知渲染，建议发布前真机过一遍。
